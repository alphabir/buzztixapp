{
  "version": 2,
  "builds": [
    {
      "src": "api/trpc.ts",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/trpc/(.*)",
      "dest": "/api/trpc.ts"
    },
    {
      "src": "/api/(.*)",
      "dest": "/api/trpc.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/$1"
    }
  ]
}